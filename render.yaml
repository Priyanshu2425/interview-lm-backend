# Render blueprint. The database is not here on purpose: it is Neon, it holds
# Evidence, and Evidence outlives any one deployment (ADR-0003). A database
# declared in the same file as the service it serves is a database that can be
# destroyed by editing that file.
services:
  - type: web
    name: interview-lm
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    # Liveness, not readiness — deliberately. `/v1/health` reads a row, and a
    # check on a timer that reads a row holds Neon's compute awake for a
    # database nobody is using, which spends the free allowance on the checking
    # rather than on Candidates. The reason to prefer the deeper check is to
    # drop a bad process out of the rotation, and on the free instance type
    # there is one instance and no rotation to drop out of. Whether the
    # database is reachable is still asked, hourly, by the keepalive Worker in
    # the gatehouse repository (ops/keepalive).
    healthCheckPath: /v1/health/live
    envVars:
      # Neon. Use the pooled endpoint — the code detects it and switches off
      # prepared statements, and points the checkpointer at the direct one.
      - key: DATABASE_URL
        sync: false
      # The Cloudflare Pages origin. Without it the API is same-origin only and
      # a browser on another host cannot call it at all (ADR-0020).
      - key: ALLOWED_ORIGINS
        sync: false
      - key: OPENROUTER_API_KEY
        sync: false
      # Never the default. The operator console is authenticated by this alone.
      - key: OPERATOR_TOKEN
        generateValue: true
      # Where a Candidate's uploaded documents live. **Not optional here.**
      # Render's filesystem is ephemeral, and since ISSUE-0033 the document is
      # the only copy of what somebody handed over — a retry re-embeds from it
      # rather than asking for the file again. Without a bucket the documents
      # live in the container and are gone at the next deploy.
      #
      # An unreachable bucket refuses the upload rather than quietly keeping it
      # somewhere it cannot be read back from (`document_store_unavailable`).
      - key: CONTENT_BUCKET
        sync: false
      # Where R2 serves the S3 API. Nothing here runs on AWS: boto3 is how R2
      # is spoken to and that is the whole of it.
      # https://<account-id>.r2.cloudflarestorage.com
      - key: R2_ENDPOINT_URL
        sync: false
      # An R2 API token with Object Read & Write on that bucket — its id and
      # its secret. Passed to the client explicitly, so a host with AWS keys in
      # its environment does not quietly become the store.
      - key: R2_ACCESS_KEY_ID
        sync: false
      - key: R2_SECRET_ACCESS_KEY
        sync: false
      # No region. R2 signs everything `auto`, so it is a constant in the code
      # rather than a variable with one legal setting.
      # Wraps every BYOK key's data key. Generated once, here, and then left
      # alone: it is not a rotating secret, it is the only thing that can read
      # what Candidates attached. Losing it does not break the deployment — it
      # makes every stored key permanently undecryptable, with the rows intact
      # and nothing able to read them. `rotate_kek` re-wraps without touching
      # ciphertext, which is how this is changed on purpose rather than by
      # accident.
      - key: BYOK_KEK
        generateValue: true
      # Identity is Gatehouse's (ADR-0026). The issuer is configuration and not
      # a constant because a development Gatehouse issues under its own name;
      # the audience is our tenant slug, which is permanent and is what stops a
      # token minted for another product being accepted here.
      - key: GATEHOUSE_ISSUER
        value: https://auth.buildspacelabs.com
      - key: GATEHOUSE_AUDIENCE
        value: interview-lm
      # Ingest embeds, and Related Topics compares the centroids that ingest
      # wrote (ADR-0021) — so this is what decides whether neighbours mean
      # anything. `openrouter` is the paid route (ADR-0019): real neighbours,
      # metered per call, and the same key the Interviewer grades with. The
      # shared Corpus in Neon is already embedded in this space; a change of
      # provider here means re-embedding it (scripts/reset_embeddings.py).
      - key: EMBEDDING_PROVIDER
        value: openrouter
      # The paid route is refused without this flag said out loud (ADR-0016):
      # a deployment must decide to spend, never start by default.
      - key: EMBEDDING_ALLOW_PAID
        value: "1"
